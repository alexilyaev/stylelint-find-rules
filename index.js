const one = 1;

let obj = { one: one };

function doSomething() {
  return [
    Object.keys(obj),
    Object.keys(obj),
    Object.keys(obj),
    Object.keys(obj),
    Object.keys(obj),
    Object.keys(obj)
  ];
}
